Get-ChildItem -Path "YOURPATHHERE" -Recurse -File | ForEach-Object { $_.FullName; Get-Content $_.FullName } | clip

replace YOURPATHHERE with the path of the folder you want to make the treeview than execute in the PowerShell console and
treeview + file content will be copied in your clipboard.
